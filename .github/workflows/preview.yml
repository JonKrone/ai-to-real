name: Preview Deployment
env:
  VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
  # VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

on:
  push:
    paths:
      - 'apps/**'
      - '.github/workflows/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install Dependencies
        run: npm ci
      - uses: tj-actions/changed-files@v45
        id: changed-files
        with:
          dir_names: true
          dir_names_exclude_current_dir: true
          dir_names_max_depth: '2'
          files: apps/**
          base_sha: 89bed326b8aeddfb29027be02fa1314fe95a6a4e
      - name: Print Changed Files
        env:
          ALL_CHANGED_FILES: ${{ steps.changed-files.outputs.all_changed_files }}
        run: |
          for file in ${ALL_CHANGED_FILES}; do
            echo "$file was changed"
          done

      # - name: Setup Project and Domain
      #   run: |
      #     CHANGED_FILES=$(git diff --name-only ${{ github.event.before }} ${{ github.sha }})
      #     APP_NAMES=$(echo "$CHANGED_FILES" | grep '^apps/' | cut -d '/' -f 2 | sort | uniq)
      #     echo "Changed apps: $APP_NAMES"
      #     echo "APP_NAMES=$APP_NAMES" >> $GITHUB_ENV
      #     # APP_PATH=$(git diff-tree --no-commit-id --name-only -r ${{ github.sha }} | grep '^apps/' | cut -d/ -f2 | uniq)
      #     # echo "APP_PATH: $APP_PATH"
      #     # if [ ! -z "$APP_PATH" ]; then
      #     #   npx tsx scripts/cli.js setup $APP_PATH
      #     # fi

      ## Preview Deployment
      # - name: Install Vercel CLI
      #   run: npm install --global vercel@latest
      # - name: Pull Vercel Environment Information
      #   run: vercel pull --yes --environment=preview --token=${{ secrets.VERCEL_TOKEN }}
      # - name: Build Project Artifacts
      #   run: vercel build --token=${{ secrets.VERCEL_TOKEN }}
      # - name: Deploy Project Artifacts to Vercel
      #   run: vercel deploy --prebuilt --token=${{ secrets.VERCEL_TOKEN }}

      # - name: Deploy App
      #   env:
      #     VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      #     # CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      #     # SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
      #   run: |
      #     APP_PATH=$(git diff-tree --no-commit-id --name-only -r ${{ github.sha }} | grep '^apps/' | cut -d/ -f2 | uniq)
      #     if [ ! -z "$APP_PATH" ]; then
      #       npx tsx scripts/pipeline/deployApp.ts $APP_PATH
      #     fi
